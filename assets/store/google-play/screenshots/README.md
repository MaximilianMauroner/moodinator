# Final Google Play phone screenshots

The five PNGs for the approved listing are pending. Older captures were moved to
`../historical/` so they cannot be mistaken for current upload assets.

Capture from the final accepted build, using fabricated mood data only, in this
order:

1. `01-home.png` — populated Home with recent entries and visible navigation.
2. `02-quick-entry.png` — the current quick-entry flow after selecting a mood.
3. `03-history.png` — searchable/browsable history with fabricated entries.
4. `04-insights.png` — current Insights with enough entries for the shown summaries.
5. `05-data-export.png` — data export and the user-selected sharing choice.

Use one consistent app theme and matching status and Android navigation bars.
Before replacing this README's pending state, record the exact build/source SHA,
package, version/code, signing identity, device/API, capture date, and synthetic
fixture in #52. Check every image for identifying or personal data and verify
that its visible controls and claims match the accepted build. A QA package may
help draft scenes but is not evidence of Play App Signing or a Play install.
